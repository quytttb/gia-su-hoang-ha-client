rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES FOR MIGRATION - REPLACE AFTER MIGRATION COMPLETES
    
    // Allow all reads and writes during migration
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 